// Types generated from the backend OpenAPI spec (sevenone-housekeeping-service).
//
// Regenerate with the backend running locally:
//   pnpm gen:api
//
// This is a placeholder until the first generation so the app type-checks before
// the backend is reachable. Phase 2 replaces it with the real generated paths.
export interface paths {}
export type components = Record<string, never>
