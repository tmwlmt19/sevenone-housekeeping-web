import { PagePlaceholder } from '@/components/page-placeholder'

export function MyTasksPage() {
  return (
    <PagePlaceholder
      title="My Tasks"
      description="Assigned tasks with one-tap status updates. Built in the housekeeper phase."
    />
  )
}
